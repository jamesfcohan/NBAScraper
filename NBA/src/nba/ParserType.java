package nba;
/*
 * simple enum that tells App class in which of the two possible ways it should parse the data
 */
public enum ParserType {
	PointsPerOffensiveReboundParser, OffensiveReboundingPercentageParser
}
